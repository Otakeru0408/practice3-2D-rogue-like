#include "UIGauge.h"

UIGauge::UIGauge()
	:x(0), y(0), w(100), h(10), paddingY(50)
{
	/*x = m_trans->x;
	y = m_trans->y;*/
}

void UIGauge::Update() {

}

void UIGauge::Draw() {
	//DrawBox(x - w / 2, y - h / 2 + paddingY, x + w / 2, y + h / 2 + paddingY, GetColor(0, 0, 0), TRUE);
}